apiVersion: 1

datasources:
  # The uid is referenced by every provisioned dashboard, so renaming it breaks them.
  - name: nexus-prometheus
    uid: nexus-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
      timeInterval: 15s
